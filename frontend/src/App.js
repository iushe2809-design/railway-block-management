import { useEffect, useMemo, useState } from "react";
import axios from "axios";
import { BrowserRouter, useLocation, useNavigate } from "react-router-dom";
import { Bell, CalendarDays, Check, ChevronRight, ClipboardList, Clock3, FileBarChart, LayoutDashboard, LogOut, Menu, Plus, Search, ShieldCheck, SlidersHorizontal, TrainFront, Users, X } from "lucide-react";
import "@/App.css";
import "@/rbmsInteractions";
const API=`${process.env.REACT_APP_BACKEND_URL}/api`;
const api=axios.create({baseURL:API});
function Login({onLogin}){const [id,setId]=useState("OFFICER001"),[password,setPassword]=useState("Officer@123"),[error,setError]=useState(""); const submit=async e=>{e.preventDefault();try{const r=await api.post("/auth/login",{employee_id:id,password});localStorage.setItem("rbms_token",r.data.token);onLogin(r.data.user)}catch(err){setError(err.response?.data?.detail||"Unable to sign in")}};return <main className="login-page"><div className="login-visual"><div className="brand-mark"><TrainFront size={28}/><span>RBMS</span></div><div className="login-copy"><p className="eyebrow">OPERATIONS CONTROL / 2026</p><h1>Keep every block<br/><em>inside the corridor.</em></h1><p>One trusted view for safe planning, approval and railway block execution.</p></div><div className="visual-footer"><span>Railway Block Management System</span><span>Secure operations workspace</span></div></div><form className="login-card" onSubmit={submit}><div className="login-intro"><div className="crest"><ShieldCheck size={22}/></div><div><p className="eyebrow">SIGN IN</p><h2>Welcome back</h2><p>Use your employee credentials to continue.</p></div></div><label>Employee ID<input data-testid="login-employee-id" value={id} onChange={e=>setId(e.target.value)} placeholder="e.g. OFFICER001"/></label><label>Password<input data-testid="login-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter password"/></label>{error&&<div data-testid="login-error" className="error-box">{error}</div>}<div className="remember"><label className="check"><input data-testid="remember-me" type="checkbox"/> <span>Remember me</span></label><button data-testid="forgot-password-button" type="button" className="text-button">Forgot password?</button></div><button data-testid="login-submit-button" className="primary-button wide">Sign in <ChevronRight size={17}/></button><p className="demo-hint" data-testid="demo-credentials-hint">Demo: ADMIN001 · OFFICER001 · USER001</p></form></main>}
function Shell({user,onLogout,children}){const nav=useNavigate(),loc=useLocation(); const [mobile,setMobile]=useState(false); const items=[{label:"Dashboard",icon:LayoutDashboard,path:"/"},{label:"New Block Request",icon:Plus,path:"/new"},{label:"My Requests",icon:ClipboardList,path:"/requests"},{label:"All Blocks",icon:ClipboardList,path:"/blocks"},{label:"Free Slot Finder",icon:Clock3,path:"/slots"},{label:"Calendar",icon:CalendarDays,path:"/calendar"},{label:"Section Wise",icon:SlidersHorizontal,path:"/sections"},{label:"Department Wise",icon:Users,path:"/departments"},{label:"Corridor Analysis",icon:ShieldCheck,path:"/corridor"},{label:"Reports",icon:FileBarChart,path:"/reports"}]; return <div className="app-shell"><aside className={mobile?"sidebar open":"sidebar"}><div className="side-brand"><div className="brand-symbol"><TrainFront size={19}/></div><div><b>RBMS</b><small>Railway operations</small></div><button data-testid="close-sidebar" className="icon-button mobile-only" onClick={()=>setMobile(false)}><X size={18}/></button></div><div className="rail-label">WORKSPACE</div><nav>{items.filter(i=>!(i.label==="My Requests"&&user.role!=="data_entry")&&!(i.label==="Free Slot Finder"&&user.role==="data_entry")).map(({label,icon:Icon,path})=><button data-testid={`nav-${label.toLowerCase().replaceAll(" ","-")}`} className={loc.pathname===path?"nav-item active":"nav-item"} onClick={()=>{nav(path);setMobile(false)}} key={label}><Icon size={17}/><span>{label}</span>{label==="New Block Request"&&<span className="nav-plus">+</span>}</button>)}</nav>{user.role==="admin"&&<><div className="rail-label admin-label">ADMINISTRATION</div><button data-testid="nav-user-management" className="nav-item" onClick={()=>nav("/users")}><Users size={17}/><span>User Management</span></button></>}<div className="sidebar-bottom"><div className="status-dot"><i/> System operational</div><button data-testid="logout-button" className="logout-button" onClick={onLogout}><LogOut size={16}/> Sign out</button></div></aside><section className="main-shell"><header className="topbar"><button data-testid="open-sidebar" className="icon-button mobile-only" onClick={()=>setMobile(true)}><Menu size={20}/></button><div className="crumb"><span>Operations</span><ChevronRight size={14}/><b>{loc.pathname==="/"?"Dashboard":items.find(x=>x.path===loc.pathname)?.label||"Management"}</b></div><div className="top-actions"><button data-testid="global-search-button" className="search-trigger"><Search size={17}/><span>Search blocks...</span><kbd>⌘ K</kbd></button><button data-testid="notifications-button" className="icon-button notification"><Bell size={19}/><i/></button><div className="profile"><div className="avatar">{user.name.split(" ").map(x=>x[0]).join("").slice(0,2)}</div><div><b>{user.name}</b><small>{user.role.replace("_"," ")}</small></div></div></div></header><main className="content">{children}</main></section></div>}
function Badge({children}){let c=children.toLowerCase().includes("strict")||children==="APPROVED"||children==="COMPLETED"?"green":children.includes("NEARLY")||children==="PENDING"?"yellow":children.includes("OUTSIDE")||children==="REJECTED"?"red":"orange";return <span data-testid={`status-${children.toLowerCase().replaceAll(" ","-")}`} className={`badge ${c}`}>{children}</span>}
function Dashboard({user}){const [data,setData]=useState(null);useEffect(()=>{api.get("/dashboard",{headers:{Authorization:`Bearer ${localStorage.getItem("rbms_token")}`}}).then(r=>setData(r.data))},[]);if(!data)return <div className="loading" data-testid="dashboard-loading">Loading operational view…</div>;return <><div className="page-heading"><div><p className="eyebrow">TUESDAY · 18 AUGUST 2026</p><h1>Good morning, {user.name.split(" ")[0]}</h1><p className="subhead">Here’s the current pulse of your railway block operations.</p></div><button data-testid="new-block-dashboard-button" className="primary-button" onClick={()=>window.history.pushState({},"","/new")}><Plus size={17}/> New block request</button></div><div className="kpi-grid">{[["Total blocks today",data.total,"+12.4%",CalendarDays,"blue"],["Currently active",data.active,"Live now",Clock3,"green"],["Awaiting approval",data.pending,"Needs review",ClipboardList,"yellow"],["Total block hours",`${data.hours}h`,"This month",Clock3,"navy"]].map(([label,value,trend,Icon,color])=><div className="kpi" data-testid={`kpi-${label.replaceAll(" ","-")}`} key={label}><div className={`kpi-icon ${color}`}><Icon size={18}/></div><div className="kpi-label">{label}</div><strong>{value}</strong><span className="kpi-trend">{trend}</span></div>)}</div><div className="dashboard-grid"><section className="panel chart-panel"><div className="panel-head"><div><p className="eyebrow">VOLUME OVERVIEW</p><h3>Blocks by major section</h3></div><span className="date-chip">Last 30 days <ChevronRight size={13}/></span></div><div className="bars" data-testid="section-block-chart">{data.sections.map(x=><div className="bar-row" key={x.name}><span>{x.name}</span><div><i style={{width:`${Math.max(8,x.value*22)}%`}}/></div><b>{x.value}</b></div>)}</div></section><section className="panel compliance-panel"><div className="panel-head"><div><p className="eyebrow">CORRIDOR CHECK</p><h3>Compliance snapshot</h3></div><ShieldCheck size={19} color="#16a34a"/></div>{data.compliance.map(x=><div className="compliance-row" data-testid={`compliance-${x.name.toLowerCase().replaceAll(" ","-")}`} key={x.name}><span><i className={`legend ${x.name.includes("STRICT")?"strict":x.name.includes("NEARLY")?"nearly":x.name.includes("PARTIAL")?"partial":"outside"}`}/>{x.name.replace(" CORRIDOR","")}</span><b>{x.value}</b></div>)}<div className="compliance-foot"><span>Overall compliance</span><strong>{Math.round((data.compliance[0].value+data.compliance[1].value)/Math.max(1,data.total)*100)}%</strong></div></section></div><section className="panel status-strip"><div><p className="eyebrow">STATUS DISTRIBUTION</p><h3>Block lifecycle</h3></div><div className="status-pills">{Object.entries(data.statuses).map(([k,v])=><div key={k}><Badge>{k}</Badge><b>{v}</b></div>)}</div></section></>}
function NewBlock({user}){const [meta,setMeta]=useState({sections:[],departments:[],lines:[],corridors:[]}),[form,setForm]=useState({date:"2026-08-25",major_section:"MURI - GDBR",sub_section:"Sec 1",department:user.department||"Engineering",line:"UP",description:"Track maintenance",allowed_start:"12:15",allowed_end:"14:00",cancellation_type:"Normal",progress:"",repercussion:"Nil",is_rbp:false,remarks:""}),[result,setResult]=useState(null),[saved,setSaved]=useState(false);const hdr={headers:{Authorization:`Bearer ${localStorage.getItem("rbms_token")}`}};useEffect(()=>{api.get("/meta",hdr).then(r=>setMeta(r.data))},[]);const corridor=meta.corridors.find(x=>x.major_section===form.major_section)||{corridor_start:"12:10",corridor_end:"14:10"};const update=(k,v)=>setForm({...form,[k]:v});const submit=async e=>{e.preventDefault();try{const r=await api.post("/blocks",form,hdr);setResult(r.data);setSaved(true)}catch(e){setResult({error:e.response?.data?.detail||"Could not save request"})}};return <><div className="page-heading compact"><div><p className="eyebrow">REQUEST WORKFLOW / STEP 01</p><h1>New block request</h1><p className="subhead">Enter the requested window. Corridor compliance is checked as you type.</p></div><div className="request-id">Draft <b>Not submitted</b></div></div><form className="form-layout" onSubmit={submit}><section className="panel form-panel"><div className="panel-head"><div><p className="eyebrow">BLOCK DETAILS</p><h3>Request information</h3></div><span className="required-note">* Required fields</span></div><div className="field-grid"><label>Date of block *<input data-testid="block-date-input" type="date" value={form.date} onChange={e=>update("date",e.target.value)}/></label><label>Major section *<select data-testid="major-section-select" value={form.major_section} onChange={e=>update("major_section",e.target.value)}>{(meta.sections.length?meta.sections:["MURI - GDBR"]).map(x=><option key={x}>{x}</option>)}</select></label><label>Sub section<select data-testid="sub-section-select" value={form.sub_section} onChange={e=>update("sub_section",e.target.value)}><option>Sec 1</option><option>Sec 2</option><option>Sec 3</option><option>Sec A</option></select></label><label>Department *<select data-testid="department-select" value={form.department} onChange={e=>update("department",e.target.value)}>{meta.departments.map(x=><option key={x}>{x}</option>)}</select></label><label>Line *<select data-testid="line-select" value={form.line} onChange={e=>update("line",e.target.value)}>{meta.lines.map(x=><option key={x}>{x}</option>)}</select></label><label className="span-2">Block description *<input data-testid="block-description-input" value={form.description} onChange={e=>update("description",e.target.value)} placeholder="What work will be carried out?"/></label></div><div className="section-divider"><span>Timing details</span></div><div className="field-grid timing-grid"><label>Allowed from *<input data-testid="allowed-from-input" type="time" value={form.allowed_start} onChange={e=>update("allowed_start",e.target.value)}/></label><label>Allowed upto *<input data-testid="allowed-upto-input" type="time" value={form.allowed_end} onChange={e=>update("allowed_end",e.target.value)}/></label><div className="duration-box" data-testid="calculated-duration"><Clock3 size={17}/><span>Calculated duration<strong>{form.allowed_start&&form.allowed_end?durationLocal(form.allowed_start,form.allowed_end):"—"}</strong></span></div></div><div className="section-divider"><span>Execution details</span></div><div className="field-grid"><label>Cancellation type<select data-testid="cancellation-type-select" value={form.cancellation_type} onChange={e=>update("cancellation_type",e.target.value)}><option>Normal</option><option>RT</option><option>BT</option><option>Burst</option></select></label><label>Repercussion<select data-testid="repercussion-select" value={form.repercussion} onChange={e=>update("repercussion",e.target.value)}><option>Nil</option><option>Minor Delay</option><option>Major Delay</option><option>Cancellation</option><option>Rescheduling</option></select></label><label>Progress made<input data-testid="progress-input" value={form.progress} onChange={e=>update("progress",e.target.value)} placeholder="e.g. 1.5 km / 1 signal"/></label><label className="toggle-label"><input data-testid="rbp-checkbox" type="checkbox" checked={form.is_rbp} onChange={e=>update("is_rbp",e.target.checked)}/><span>RBP block</span></label><label className="span-2">Remarks<textarea data-testid="remarks-input" value={form.remarks} onChange={e=>update("remarks",e.target.value)} placeholder="Add operational notes"/></label></div><div className="form-actions"><span>Fields are auto-saved locally</span><button data-testid="submit-block-button" className="primary-button" type="submit"><Check size={17}/> Submit for approval</button></div></section><aside className="validation-rail"><div className="panel validation-card"><div className="validation-title"><div><p className="eyebrow">LIVE VALIDATION</p><h3>Corridor status</h3></div><span className="pulse"/></div><div className={`validation-status ${corridorStatusLocal(form.allowed_start,form.allowed_end,corridor.corridor_start,corridor.corridor_end).color}`} data-testid="live-corridor-status"><strong>{corridorStatusLocal(form.allowed_start,form.allowed_end,corridor.corridor_start,corridor.corridor_end).label}</strong><span>Auto-checked against corridor</span></div><div className="corridor-times"><div><small>Corridor opens</small><b>{corridor.corridor_start}</b></div><div><small>Corridor closes</small><b>{corridor.corridor_end}</b></div></div><div className="margin-note"><ShieldCheck size={16}/><span>Nearly Corridor margin<strong>±30 minutes</strong></span></div></div>{result&&<div className={result.error?"panel error-box":"panel success-card"} data-testid="block-submit-result">{result.error||`Request ${result.block.block_id} submitted as PENDING`}</div>}</aside></form></>}
function durationLocal(s,e){let d=Math.max(0,(+e.split(":")[0]*60+ +e.split(":")[1])-(+s.split(":")[0]*60+ +s.split(":")[1]));return `${Math.floor(d/60)}h ${String(d%60).padStart(2,"0")}m`};function corridorStatusLocal(s,e,cs,ce){let a=+s.slice(0,2)*60+ +s.slice(3),b=+e.slice(0,2)*60+ +e.slice(3),c=+cs.slice(0,2)*60+ +cs.slice(3),d=+ce.slice(0,2)*60+ +ce.slice(3);if(a>=c&&b<=d)return {label:"STRICTLY INSIDE CORRIDOR",color:"green"};if(a<=d&&b>=c&&a>=c-30&&b<=d+30)return {label:"NEARLY CORRIDOR",color:"yellow"};if(a<=d&&b>=c)return {label:"PARTIAL OVERLAP",color:"orange"};return {label:"COMPLETELY OUTSIDE CORRIDOR",color:"red"}}
function Blocks({user}){const [blocks,setBlocks]=useState([]),[search,setSearch]=useState(""),[status,setStatus]=useState("");const hdr={headers:{Authorization:`Bearer ${localStorage.getItem("rbms_token")}`}};useEffect(()=>{api.get(`/blocks?search=${search}&status=${status}`,hdr).then(r=>setBlocks(r.data))},[search,status]);const decide=async(b,action)=>{let reason=action==="REJECTED"?prompt("Rejection reason")||"No reason provided":"";await api.patch(`/blocks/${b.id}`,{reason:`${action}|${reason}`},hdr);setBlocks(blocks.map(x=>x.id===b.id?{...x,status:action}:x))};return <><div className="page-heading compact"><div><p className="eyebrow">OPERATIONS REGISTER</p><h1>{user.role==="data_entry"?"My requests":"All blocks"}</h1><p className="subhead">Search, review and action railway block records.</p></div><button data-testid="export-csv-button" className="secondary-button" onClick={()=>window.open(`${API}/reports/export`)}><FileBarChart size={16}/> Export CSV</button></div><section className="panel table-panel"><div className="table-toolbar"><div className="search-input"><Search size={17}/><input data-testid="blocks-search-input" placeholder="Search ID, section, department…" value={search} onChange={e=>setSearch(e.target.value)}/></div><select data-testid="blocks-status-filter" value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option><option>PENDING</option><option>APPROVED</option><option>REJECTED</option><option>COMPLETED</option></select></div><div className="table-wrap"><table><thead><tr><th>Block ID</th><th>Date / section</th><th>Department</th><th>Window</th><th>Compliance</th><th>Status</th>{user.role!=="data_entry"&&<th>Action</th>}</tr></thead><tbody>{blocks.map(b=><tr data-testid={`block-row-${b.block_id}`} key={b.id}><td><b className="mono">{b.block_id}</b><small>{b.description}</small></td><td><b>{b.date}</b><small>{b.major_section} · {b.line}</small></td><td>{b.department}</td><td><b>{b.allowed_start} – {b.allowed_end}</b><small>{b.duration}</small></td><td><Badge>{b.corridor_status}</Badge></td><td><Badge>{b.status}</Badge></td>{user.role!=="data_entry"&&<td>{b.status==="PENDING"?<div className="row-actions"><button data-testid={`approve-${b.block_id}`} className="icon-action approve" onClick={()=>decide(b,"APPROVED")}><Check size={15}/></button><button data-testid={`reject-${b.block_id}`} className="icon-action reject" onClick={()=>decide(b,"REJECTED")}><X size={15}/></button></div>:<span className="muted">Reviewed</span>}</td>}</tr>)}</tbody></table>{!blocks.length&&<div className="empty" data-testid="blocks-empty-state">No blocks match your filters.</div>}</div></section></>}
function Slots(){const [date,setDate]=useState("2026-08-25"),[section,setSection]=useState("MURI - GDBR"),[slots,setSlots]=useState([]),[meta,setMeta]=useState({sections:[]});const hdr={headers:{Authorization:`Bearer ${localStorage.getItem("rbms_token")}`}};useEffect(()=>{api.get("/meta",hdr).then(r=>setMeta(r.data))},[]);const find=()=>api.get(`/slots?date=${date}&major_section=${encodeURIComponent(section)}&line=UP&min_duration=60`,hdr).then(r=>setSlots(r.data));return <><div className="page-heading compact"><div><p className="eyebrow">PLANNING TOOL</p><h1>Free corridor / slot finder</h1><p className="subhead">Find an available operating window in seconds.</p></div></div><section className="panel finder-panel"><div className="finder-fields"><label>Date<input data-testid="slot-date-input" type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Major section<select data-testid="slot-section-select" value={section} onChange={e=>setSection(e.target.value)}>{(meta.sections.length?meta.sections:[section]).map(x=><option key={x}>{x}</option>)}</select></label><label>Line<select data-testid="slot-line-select"><option>UP</option><option>DN</option><option>Single</option></select></label><button data-testid="find-slots-button" className="primary-button" onClick={find}><Search size={16}/> Find slots</button></div></section>{slots.length>0&&<section className="panel slots-panel"><div className="panel-head"><div><p className="eyebrow">AVAILABILITY RESULT</p><h3>{section} · {date}</h3></div><span className="result-count">{slots.filter(x=>x.status==="Available").length} available windows</span></div><div className="slot-list">{slots.map((s,i)=><div className={s.status==="Available"?"slot-row available":"slot-row occupied"} data-testid={`slot-row-${i}`} key={i}><div className="slot-time"><Clock3 size={17}/><b>{s.start} – {s.end}</b></div><span>{s.duration}</span><Badge>{s.status}</Badge>{s.status==="Available"&&<button data-testid={`use-slot-${i}`} className="use-slot" onClick={()=>window.location.assign(`/new?start=${s.start}&end=${s.end}`)}>Use this slot <ChevronRight size={15}/></button>}</div>)}</div></section>}</>}
function SimplePage({title,eyebrow,children}){return <><div className="page-heading compact"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="subhead">Operational intelligence for safer, faster block decisions.</p></div></div><section className="panel placeholder-panel">{children||<><div className="placeholder-icon"><CalendarDays size={22}/></div><h3>Workspace ready</h3><p>Use the navigation to review live block records and planning data.</p></>}</section></>}
function AppInner(){const [user,setUser]=useState(null),[checking,setChecking]=useState(true);const location=useLocation();useEffect(()=>{const t=localStorage.getItem("rbms_token");if(t)api.get("/auth/me",{headers:{Authorization:`Bearer ${t}`}}).then(r=>setUser(r.data)).catch(()=>localStorage.removeItem("rbms_token")).finally(()=>setChecking(false));else setChecking(false)},[]);if(checking)return <div className="loading">Opening secure workspace…</div>;if(!user)return <Login onLogin={setUser}/>;const path=location.pathname;let page=path==="/"?<Dashboard user={user}/>:path==="/new"?<NewBlock user={user}/>:path==="/slots"?(user.role==="data_entry"?<SimplePage title="Access restricted" eyebrow="OFFICER WORKSPACE"><div className="placeholder-icon"><ShieldCheck size={22}/></div><h3>Officer access required</h3><p data-testid="slot-access-denied">Free slot planning is available to officers and administrators.</p></SimplePage>:<Slots/>):path.includes("blocks")||path.includes("requests")?<Blocks user={user}/>:<SimplePage title={path.includes("calendar")?"Calendar & timeline":path.includes("department")?"Department wise analysis":path.includes("section")?"Section wise blocks":path.includes("report")?"Reports":"Corridor analysis"} eyebrow="ANALYTICS WORKSPACE"/>;return <Shell user={user} onLogout={()=>{localStorage.removeItem("rbms_token");setUser(null)}}>{page}</Shell>};export default function App(){return <BrowserRouter><AppInner/></BrowserRouter>}